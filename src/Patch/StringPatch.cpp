#include "StringPatch.h"

#include <string>
#include <fstream>
#include <unordered_map>

using namespace std;

using MapOffStrType = unordered_map<int, string>;

bool StringPatch::LoadStrings(const char * fileNmae)
{
	return false;
}

bool StringPatch::Apply(void * start, int size, const char * pattern)
{
	return false;
}
